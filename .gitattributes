* text=auto eol=lf
*.bat text eol=lf

# Don't diff machine generated files
poetry.lock -diff

# Don't include next files in archives
.gitignore export-ignore
.gitattributes export-ignore
.github export-ignore
